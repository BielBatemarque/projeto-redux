
import { Card } from "../Card/Card";

export const Sorteio = (props) => {

    return(
        <Card title={'Sorteio de um Número'} purple>
            <div>
                <span>
                    <span>Resultado:</span>
                    <strong>10</strong>
                </span>
            </div>
        </Card>

    );
}